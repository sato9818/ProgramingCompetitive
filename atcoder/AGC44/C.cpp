#include <bits/stdc++.h>
#define rep(i,n) for (int i=0;i<(n);i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main(void){
    int N;
    string T;
    
    cin >> N;
    cin >> T;
    int sum = pow(3, N);
    vector<int> A(sum);

    cout << sum << endl;

    return 0;
}