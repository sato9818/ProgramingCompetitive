#include <bits/stdc++.h>
#define rep(i,n) for (int i=0;i<(n);i++)
#define rep1(i,n) for (int i=1;i<(n);i++)
using namespace std;
using ll = long long;
using P = pair<int, ll>;

const int MOD = 998244353;
const int INF = 1001001001;
const ll LINF = 1001001001001001001LL;

ll square_sum(int num){
    num %= MOD;
    ((num + 1) * num / 2) % MOD - 
}

int main() {
    int n,m,q;
    cin >> n >> m;
    cin >> q;

    ll ans = 0;

    int a,b,c,d;
    cin >> a >> b >> c >> d;


    return 0;
}
