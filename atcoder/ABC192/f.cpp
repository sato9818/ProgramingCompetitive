#include <bits/stdc++.h>
#define rep(i,n) for (int i=0;i<(n);i++)
#define rep1(i,n) for (int i=1;i<(n);i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
  int n;
  ll x;
  vector<int> a(n);
  rep(i,n){
    cin >> a[i];
  }
  rep(i,n){
    rep()
  }
  string n;
  cin >> n >> k;
  
  rep(i,k){
    
  }

  cout << n << endl;
  return 0;
}